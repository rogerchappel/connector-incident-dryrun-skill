# Security

Do not paste secrets, private customer details, access tokens, or confidential incident transcripts into fixture briefs. Prefer redacted messages and local evidence labels.

This tool never performs live connector writes. If a downstream workflow executes the generated plan, keep approval and audit controls outside this package.
